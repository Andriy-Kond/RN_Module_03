// // it is operations for createScreen from createReducer

// export const capturedPhoto = (photo) => async (dispatch, getState) => {
// 	try {
// 		await dispatch(createScreenSlice.actions.capturedPhoto(photo));
// 	} catch (error) {
// 		console.log("authSingInUser >> errorCode:", error.code);
// 		console.log("authSingInUser >> errorMessage:", error.message);
// 	}
// };

// export const prevCapturedPhoto = (photo) => async (dispatch, getState) => {
// 	try {
// 		await dispatch(createScreenSlice.actions.prevCapturedPhoto(photo));
// 	} catch (error) {
// 		console.log("authSingInUser >> errorCode:", error.code);
// 		console.log("authSingInUser >> errorMessage:", error.message);
// 	}
// };

// export const createBtnState = (btnState) => async (dispatch, getState) => {
// 	try {
// 		await dispatch(createScreenSlice.actions.btnSendState(btnState));
// 	} catch (error) {
// 		console.log("authSingInUser >> errorCode:", error.code);
// 		console.log("authSingInUser >> errorMessage:", error.message);
// 	}
// };

// export const createClearStates = () => async (dispatch, getState) => {
// 	await dispatch(createScreenSlice.actions.clearStates());
// };
